name: helm-quality

on:
  workflow_call:
    inputs:
      paths:
        type: string
        required: true

jobs:

  helm-quality:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        paths: ${{ fromJSON(inputs.paths) }}

    steps:
    - uses: actions/checkout@v5
    - run: helm lint ${{ matrix.paths }}
